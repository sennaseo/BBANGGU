# 프로덕션 빌드 — nginx 뒤에서 서빙되므로 전부 상대경로 (도메인/IP가 바뀌어도 재빌드 불필요)
# /api → 백엔드, / → nginx 정적 /uploads
VITE_API_BASE_URL=/api
VITE_IMAGE_BASE_URL=/
VITE_PUBLIC_URL=/
VITE_APP_ENV=production
# VITE_KAKAO_MAP_API_KEY 는 도커 빌드 arg로 주입 (서버 .env)
